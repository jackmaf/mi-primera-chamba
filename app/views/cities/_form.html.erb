<%= form_with(model: city) do |form| %>
  <% if city.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
        <% city.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, "Nombre", class: "form-label" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= form.label :code_postal, "Código Postal", class: "form-label" %>
    <%= form.text_field :code_postal, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= form.label :department_id, "Departamento", class: "form-label" %>
    <%= form.collection_select :department_id, Department.all, :id, :name, { prompt: "Seleccione un departamento" }, { class: "form-select" } %>
  </div>

  <div>
    <%= form.submit "Guardar", class: "btn btn-primary" %>
  </div>
<% end %> 